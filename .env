JWT_SECRET=mySuperSecretKey

# Secret key for JWT token generation
JWT_SECRET=your_jwt_secret_key_here

# Salt rounds for bcrypt password hashing
SALT_ROUNDS=10

# Port on which the server will run (optional, defaults to 3000)
PORT=3000
